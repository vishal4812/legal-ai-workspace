# Infrastructure

The root `docker-compose.yml` is the source of truth for local infrastructure. Put future production manifests, reverse-proxy configuration, and observability assets here; do not duplicate local credentials or persistent data in this directory.
