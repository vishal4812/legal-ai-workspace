# Architecture

LEGAL MASTER uses a modular monolith to keep local development simple while preserving explicit boundaries.

```text
React UI -> FastAPI API -> application modules -> PostgreSQL
                                     |         -> private document storage
                                     |         -> local PDF/DOCX extractors
                                     |              `-> bounded Tesseract PDF OCR fallback
                                     |         -> deterministic document chunks
                                     |         -> Redis (future jobs)
                                     |         -> local CPU embedding provider
                                     `---------> private Qdrant chunk vectors
```

## Boundary rules

- API routers validate transport data and delegate work; they do not perform storage or database operations directly.
- Services coordinate use cases, repositories own SQLAlchemy queries, and models remain persistence-focused.
- Every workspace-scoped query includes workspace authorization. Vector payloads carry workspace, case, document, chunk, extraction SHA, hash, and page identifiers; retrieval revalidates canonical rows after the filtered Qdrant query.
- Storage keys are generated by the application. Original filenames are metadata only and never become filesystem paths.
- AI, extraction, OCR, chunking, storage, and vector integrations depend on internal interfaces. Provider-specific clients stay at the boundary.
- Background processing remains part of the same codebase and transaction model; Redis-backed workers can be added when ingestion arrives.
- Extraction routes delegate to `DocumentExtractionService`; parser implementations never live in route handlers.

### Authentication boundary

Phase 2 authentication uses Argon2id password hashes and two JWT types. Short-lived access tokens are stateless bearer credentials. Refresh tokens are rotated on every use; their `jti` values are stored in PostgreSQL so logout, expiration, and reuse can be enforced without storing raw token material. Browser refresh tokens use an HttpOnly cookie while access tokens remain in memory.

### Workspace and document authorization boundary

The tenant chain is `Document -> Case -> Workspace -> WorkspaceMember -> User`. `get_workspace_access` resolves a workspace only through the authenticated user's membership and returns 404 for non-members. `get_case_access` loads a case by both `case_id` and the authorized `workspace_id`; `get_document_access` then loads by both `document_id` and the authorized `case_id`. Direct IDs, cross-tenant requests, and any workspace/case/document mismatch therefore return 404 without revealing object existence.

For documents, OWNER, ADMIN, and MEMBER can upload, view, download, and archive; VIEWER can view and download. The API enforces the matrix independently of the UI.

### Private document storage boundary

```text
DocumentService -> StorageProvider -> LocalStorageProvider -> private Docker volume
```

The provider exposes staged writes, open, size, existence, and deletion primitives over opaque keys. Local storage generates temporary files beneath a private staging directory, fsyncs and atomically moves completed uploads, and resolves every key before access to prove it remains beneath `DOCUMENT_STORAGE_PATH`. Keys are UUID-derived as `workspace/case/document/stored-id`; original filenames remain metadata only. The abstraction can be replaced by S3, MinIO, or another object store without changing document records or nested API contracts.

Upload memory is bounded by 1 MiB chunks and `DOCUMENT_MAX_SIZE_BYTES` (50 MiB by default). SHA-256 is updated for each chunk. PDF and DOCX signatures/packages are validated from the staged original before publication. A storage failure produces no database row; a database failure after publication triggers object cleanup.

Documents have one `UPLOADED` lifecycle status in Phase 4. Archive sets `is_active=false` without deleting the database row or physical original. Archived originals remain downloadable by authorized members. Domain foreign keys use `RESTRICT`; no retention scheduler or permanent deletion exists.

### Document extraction boundary

```text
Authorized DocumentAccess
        -> DocumentExtractionService
        -> StorageProvider.open(opaque key)
        -> PDFExtractor | DOCXExtractor
        -> normalized DocumentExtraction in PostgreSQL
```

`DocumentExtractionService` is independent of FastAPI and owns extractor selection, lifecycle transitions, normalization, persistence, idempotency, and safe failure recording. Extraction is synchronous for the Phase 6 MVP, while the service contract remains callable from a future Redis worker. The original is opened read-only through the existing provider; no service receives the storage root or arbitrary filesystem path, and extraction never modifies the object, document storage key, or Phase 4 SHA-256 metadata.

`document_extractions` is a one-to-one child of `documents`. It stores actual parser package/version, `PENDING`, `PROCESSING`, `COMPLETED`, or `FAILED` status, normalized text, character/page counts, extraction time, safe failure metadata, and `source_sha256_hash`. Completed requests return the existing row. Failed rows are reset and reused on retry. Pending/processing requests conflict rather than creating duplicates.

For PDFs, PyMuPDF first returns selectable text per one-based page. `is_meaningful_text` deterministically requires at least 20 non-whitespace characters, 10 alphabetic characters, 90% printable content, and 50% alphanumeric content. Sufficient pages remain direct text. Insufficient pages are OCR candidates, including pages containing only page numbers, short headers, or parser noise.

```text
PDF -> PyMuPDF page text -> sufficient page -> direct page text
                         `-> insufficient page -> render at configured DPI
                                                -> OCRProvider
                                                -> TesseractOCRProvider
                                                -> OCR page text
```

The extractor rejects a PDF whose total page count exceeds `OCR_MAX_PAGES` before scanning or rendering pages, then makes one detection pass and renders only OCR candidates sequentially. It predicts and checks rendered pixel count before allocation, retains only one RGB page image, calls Tesseract without `shell=True`, and releases the image before continuing. A shared provider semaphore bounds simultaneous OCR subprocesses. One deadline covers engine/language probes, concurrency waiting, rendering, and recognition. A limit failure fails the whole extraction rather than completing partially.

After the existing safe normalization, PDF text is persisted with `[Page N]` markers in original order and actual total PDF `page_count`. Mixed documents combine direct and OCR pages without OCRing sufficient pages. `python-docx` remains direct-only, walks body paragraphs/tables in order, uses ` | ` table separators, and stores no visual page count.

The focused Phase 6 migration adds only `parser_metadata` JSON to the existing one-to-one record. A completed result's `method` is `direct_text`, `ocr`, or `mixed`; a page-limit rejection is `undetermined` because it occurs before page inspection. PDF metadata includes actual engines/versions, configured language/DPI when applicable, and one-based `direct_text_pages`/`ocr_pages`. No separate `ocr_results` table exists.

Tesseract availability and configured languages are checked only when a page needs OCR, so a missing OCR runtime never prevents application startup, authentication, uploads, DOCX extraction, or sufficient direct-PDF extraction. There is no language fallback. Failures persist stable codes and safe messages; raw Tesseract output, command lines, document text, storage keys, paths, and traces are not logged or returned. Completed results are idempotent, failed rows are reused on retry, and the original binary/hash remain immutable.

Normalization only converts line endings, removes nulls, reduces horizontal whitespace/excess blank lines, and trims mechanical line-edge whitespace. Legal words, numbers, clauses, headings, punctuation, and repeated content are not rewritten or removed.

OWNER, ADMIN, and MEMBER may trigger extraction; all four workspace roles may retrieve it. Non-members and cross-boundary IDs still fail through the existing authorization chain with 404. Parser errors become bounded failure codes/messages; API responses and application logs do not include document contents, storage keys, or parser-supplied paths.

### Chunking, embedding, and vector boundaries

```text
Completed DocumentExtraction
        -> DocumentIndexingService
        -> DeterministicLegalChunker
        -> document_chunks (canonical text, PostgreSQL)
        -> EmbeddingProvider -> LocalEmbeddingProvider -> FastEmbed/ONNX CPU
        -> VectorStore -> QdrantVectorStore -> document chunk points

Authorized WorkspaceAccess
        -> VectorSearchService
        -> local query embedding
        -> Qdrant workspace/case payload filter
        -> tenant-scoped canonical PostgreSQL chunk verification
        -> ranked chunks only
```

`DeterministicLegalChunker` treats each non-whitespace lexeme as a stable token approximation. It targets 800 lexemes, overlaps 120, avoids a final fragment below 100 where possible, prefers page/paragraph/sentence endings, and otherwise ends at a word boundary. It takes exact normalized extraction text, does not paraphrase/remove legal content, and emits stable order plus nullable page ranges derived from preserved `[Page N]` markers. Chunk SHA-256 is computed after final content selection from the exact persisted UTF-8 bytes.

`document_chunks` has restrictive foreign keys to document and extraction, `UNIQUE(document_id, chunk_index)`, document/extraction/hash indexes, exact canonical content, counts, page range, and safe JSON metadata. Qdrant uses the chunk UUID as point ID and does not duplicate content. Payloads contain only tenant/source identifiers, chunk order/range, hashes, source type, and extraction method—never storage keys, filenames, tokens, or passwords.

`LocalEmbeddingProvider` lazily loads configurable `jinaai/jina-embeddings-v2-small-en` through FastEmbed. It discovers the supported model dimension (512) from library metadata, validates it against configuration and each result, rejects non-finite/malformed vectors, supports query/passage batches, and bounds CPU inference concurrency. Its 8,192-token model context safely exceeds the default approximate chunk size. Model files live in the private `embedding_model_cache` volume and first-use download is the only network requirement; inference needs no API key, paid service, GPU, Ollama, or LLM.

`QdrantVectorStore` creates only a missing collection with 512-dimensional cosine vectors and UUID payload indexes for workspace/case/document filters. Existing collection dimension and distance are verified. Incompatibility is a safe error; the adapter never destroys/recreates a collection. Compose does not publish Qdrant ports to the host, so authenticated FastAPI routes are the only application retrieval surface.

`document_indexes` is one-to-one with documents and owns `PENDING`, `PROCESSING`, `COMPLETED`, and `FAILED`, provider/model/dimension, expected chunk count, normalized extraction SHA-256, collection, timestamps, and bounded error fields. Completed current requests are idempotent; processing conflicts; failed/stale rows are reused. Reindex commits non-completed state, transactionally replaces PostgreSQL chunks, embeds/upserts in bounded batches, verifies exact Qdrant point count, then marks completed. A failure remains non-completed. Search joins only completed canonical index rows and checks payload workspace/case/document/content/extraction hashes, preventing stale or partially written points from being returned without pretending PostgreSQL and Qdrant share a distributed transaction.

Search authorization precedes embedding and vector access. Optional case filters are resolved through the authorized workspace before Qdrant, every vector query has mandatory `workspace_id`, and PostgreSQL tenant joins recheck every returned chunk. OWNER/ADMIN/MEMBER may mutate index state; VIEWER may view status and search; outsiders and nested mismatches receive 404.

Document archival retains original/extraction/chunk/index records under the existing legal-domain policy. Authorized members may continue to retrieve semantic matches from an archived retained document; Phase 7 adds no hard-delete or automated vector-retention workflow.

## Current runtime

The API exposes `/health`, authentication, workspaces/memberships, nested cases/documents/extraction/indexing, and authenticated workspace semantic search. PostgreSQL contains canonical identity/domain/document/extraction/chunk/index state. The private document and embedding-cache volumes are backend-only. Qdrant is reachable only inside the Compose network.

The Document Vault UI lives at `/workspaces/:workspaceId/cases/:caseId/documents`; its read-only extraction view is nested at `/workspaces/:workspaceId/cases/:caseId/documents/:documentId/extraction`. The vault shows role-aware index lifecycle controls and labels retrieval as “Semantic Search Results.” It never presents a generated answer or receives a storage key/path/vector.

Phase 7 stops at vector retrieval. RAG answer generation, LLM APIs, chat, conversational memory, generated citations, summarization, reranking with an LLM, legal advice, and legal analysis remain intentionally absent. Phase 8 is not implemented.
